[project]
name = "project-tracker"
version = "0.1.0.dev0"
description = "`project-tracker` aims to be a simple and lightweight Project Tracking application"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["zensical>=0.0.32"]
